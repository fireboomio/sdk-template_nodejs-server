import type { HookServerConfiguration } from '@fireboom/server'

export const hookServerConfiguration: HookServerConfiguration = {
  listen: {
    host: '{{getVariableString serverOptions.listen.host}}',
    port: {{getVariableString serverOptions.listen.port}}
  },
  logLevel: '{{getVariableString serverOptions.logger.level}}',
  apiBaseURL: '{{internalBaseURL}}',
  envFilePath: '{{nodeEnvFilepath}}'
}